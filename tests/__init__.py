"""
Podex Agent Integration Tests

These tests are designed to run locally with Ollama and test the complete
agent system including all agent types, modes, custom agents, and UI integration.

To run: make test-agent

Note: These tests are marked with @pytest.mark.local_only and will be skipped in CI.
"""
