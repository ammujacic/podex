/**
 * Adapter interfaces for platform-agnostic API client.
 */

export type {
  ApiClientConfig,
  AuthProvider,
  ErrorReporter,
  HttpAdapter,
  HttpRequestConfig,
  HttpResponse,
  StorageAdapter,
} from './types';
