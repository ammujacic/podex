/**
 * Theme exports for CLI components.
 */

export { colors, terminalColors, type Colors, type TerminalColors } from './colors';
export {
  spacing,
  borders,
  icons,
  animation,
  type Spacing,
  type Borders,
  type Icons,
  type Animation,
} from './tokens';
export {
  ThemeProvider,
  useTheme,
  theme,
  type Theme,
  type ThemeProviderProps,
} from './ThemeProvider';
